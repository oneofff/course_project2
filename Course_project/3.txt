1. акой код позвол€ет применить некоторую операцию ко всем элементам последовательности в STL:
1)forall
2)while
3)for_each
4)for_all
3
2. акой алгоритм не €вл€етс€ стандартным алгоритмом сортировки STL?
1)std::sort
2)std::partial_sort
3)std::stable_sort
4)std::qsort
4
3.¬ каких из перечисленных контейнеров сложность поиска составл€ет O(n)?
1)std::set
2)std::vector
3)std::map
4)std::hash_set
2
4. аким образом лучше хранить массив булевских значений, использу€ следующие контейнеры из STL дл€ максимального быстродействи€?
1)std::deque
2)std::vector
3)std::set
4)std::vector<char>
4
5. аких контейнеров нет в STL?
1)multivector
2)vector
3)map
4)deque
1
5. акие есть виды итераторов?
1)¬ходные, выходные, однонаправленные, двунаправленные, произвольного доступа.
2)ƒвухмерные, однонаправленные
3)—татические, константные,однонаправленные, двунаправленные
4)¬ходные, выходные, многопоточные, двунаправленные.
1
6.¬ чем отличие std::vector::at от std::vector::operator[]?
1) std::vector::at всегда возвращает объект по значению
2)std::vector::at в некоторых случа€х может выкинуть исключение.
3)std::vector::at возвращает итератор текущего элемента.
4)std::vector::at возвращает указатель на текущий элемент.
2